name: assignment_02
description: 'A new Flutter project.'
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.0.2
  http: ^1.2.2
  intl: ^0.19.0
  mocktail: ^1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  test: ^1.25.2
  integration_test:
    sdk: flutter

flutter:
  assets:
    - assets/clear_sky.png
    - assets/hot_weather.png
    - assets/rain.png
    - assets/snow.png
  uses-material-design: true
